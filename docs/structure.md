# Structure

The tree is generated by `python check_docs.py --write` — don't edit it by hand.
The tables below are hand-written: what each file is for and what would surprise you.

<!-- GENERATED:tree -->
```
|-- bin/
|   `-- gitcat.js
|-- docs/
|   |-- architecture.html
|   |-- architecture.md
|   |-- decisions.md
|   |-- stack_and_tooling.md
|   `-- structure.md
|-- scripts/
|   |-- e2e.js
|   `-- eval.js
|-- src/
|   |-- agent/
|   |   |-- agent.js
|   |   |-- commitMessage.js
|   |   |-- context.js
|   |   |-- knownErrors.js
|   |   |-- prompt.js
|   |   `-- router.js
|   |-- catalog/
|   |   |-- branches.js
|   |   |-- changes.js
|   |   |-- github.js
|   |   |-- index.js
|   |   |-- inspect.js
|   |   |-- params.js
|   |   |-- remotes.js
|   |   |-- repo.js
|   |   `-- risk.js
|   |-- config/
|   |   `-- settings.js
|   |-- exec/
|   |   `-- run.js
|   |-- llm/
|   |   `-- index.js
|   |-- ui/
|   |   |-- components/
|   |   |   |-- Busy.js
|   |   |   |-- Confirm.js
|   |   |   |-- Header.js
|   |   |   |-- Item.js
|   |   |   `-- PromptInput.js
|   |   |-- App.js
|   |   |-- cat.js
|   |   |-- slash.js
|   |   `-- theme.js
|   `-- headless.js
|-- test/
|   |-- fixtures/
|   |   `-- fake-gh.mjs
|   |-- agent.e2e.test.js
|   |-- catalog.test.js
|   |-- parsing.test.js
|   `-- ui.test.js
|-- .env.example
|-- .gitignore
|-- check_docs.bat
|-- check_docs.py
|-- package-lock.json
|-- package.json
|-- README.md
|-- start_commands.md
|-- start_project.bat
`-- start_project.sh
```
<!-- /GENERATED:tree -->

## Entry points

| Path | What it does |
|---|---|
| `bin/gitcat.js` | Parses flags, runs `detectProviders()` (decides Ollama vs Groq), then either headless (`-p`) or the TUI. Refuses the TUI when stdin isn't a TTY. |
| `src/headless.js` | One request, plain output, exit code 1 if any command failed. `--yes` approves confirms; without a TTY it declines them. |
| `start_project.bat` | Windows launcher: prereq checks, first-run `npm install`, asks for a repo folder, keeps the window open. |
| `start_project.sh` | Same for bash/macOS/Linux. |

## Agent

| Path | What it does |
|---|---|
| `src/agent/agent.js` | Orchestrator. `askModel()` validates model steps and runs one repair round; `execute()` re-snapshots the repo between steps because later commands depend on earlier ones; `debug()` tries `knownErrors` before the model. |
| `src/agent/router.js` | Exact phrases → steps with zero model calls. `switch to X` only takes the fast path when X is a real branch, otherwise the model decides. |
| `src/agent/prompt.js` | `SYSTEM_PROMPT` is static on purpose so Ollama reuses its KV cache; `slimSystemPrompt()` is the Groq variant. |
| `src/agent/context.js` | Snapshot from `git status --porcelain=v2` + 5 parallel git calls (~100 ms). gh accounts read from gh's `hosts.yml` (instant, offline) instead of `gh auth status` (~1 s, network). |
| `src/agent/knownErrors.js` | Regex → cause + fix steps. `steps: null` means "cause known, let the model plan". Order matters: first match wins. |
| `src/agent/commitMessage.js` | Diff (truncated to 7k chars) → conventional commit message; strips empty scopes like `feat(:)`. |

## Catalog

| Path | What it does |
|---|---|
| `src/catalog/index.js` | Registry, `resolveStep()` (validation), `catalogText()` for the prompt, `selectOps()` for the slim prompt. |
| `src/catalog/params.js` | Param spec mini-language: `"str!"` required, `"list"`, `"a|b"` enum. Coerces sloppy model output (`"5"` → 5, `"a, b"` → list). |
| `src/catalog/risk.js` | Risk tiers for raw commands (`reset --hard`, `push -f`, `clean` → danger). |
| `src/catalog/inspect.js` | Read-only ops (status, log, diff, blame…). |
| `src/catalog/changes.js` | Stage/commit/undo/discard/stash. |
| `src/catalog/branches.js` | Branch/merge/rebase/conflict ops. `resolve_conflicts` concludes the merge when no conflicts remain. |
| `src/catalog/remotes.js` | Push/pull/fetch/remotes/tags. `push` adds `-u origin <branch>` only when there's no upstream. |
| `src/catalog/github.js` | gh ops: accounts, repo create/clone, PRs, issues, releases. `gh_repo_create` defaults to private; public is "danger". |
| `src/catalog/repo.js` | init/clone/config/.gitignore/worktrees and the `git_raw`/`gh_raw` escape hatch. |

## Runtime, model, UI

| Path | What it does |
|---|---|
| `src/exec/run.js` | Every command goes through here: argv (no shell), `GIT_EDITOR=true`, pagers off, `GIT_TERMINAL_PROMPT=0` so nothing hangs invisibly. |
| `src/llm/index.js` | `chat()` with provider fallback. `OLLAMA_CTX` must match between warmup and chat, or Ollama reloads the model every call. |
| `src/config/settings.js` | Loads `.env` from GitCat's folder, settings/history under `GITCAT_HOME` (default `~/.gitcat`). |
| `src/ui/App.js` | Root component. Owns the transcript (`<Static>`), confirm routing, queueing requests typed while busy. |
| `src/ui/components/PromptInput.js` | Line editor, slash menu, history. Input is gated by a render-time ref (see decisions.md). |
| `src/ui/components/Confirm.js` | Presentational confirm box + `confirmKey()` key mapping. |
| `src/ui/components/Busy.js` | Animated cat + current status + elapsed time. |
| `src/ui/components/Header.js` | Banner (cat, path, branch, model, gh account) and tips. |
| `src/ui/components/Item.js` | Renders each transcript item type (command, diagnosis, commit message, answer, help). |
| `src/ui/cat.js` | Cat frames per mood (thinking/running/debugging/writing). ASCII only so widths match on every terminal. |
| `src/ui/slash.js` | Slash commands. `prompt:` entries route through the agent's fast path; `run:` entries are local. |
| `src/ui/theme.js` | Colors, mode labels, `html` template tag. |

## Tests and scripts

| Path | What it does |
|---|---|
| `test/catalog.test.js` | Every op builds valid argv; risk + coercion + push-upstream rules. |
| `test/parsing.test.js` | Router, known errors, porcelain/hosts.yml parsing, slash matching. |
| `test/agent.e2e.test.js` | Real git in temp repos with a scripted model: rejected push auto-fix, conflicts, modes, untrack. |
| `test/ui.test.js` | Real App through ink-testing-library: slash menu, confirm y/n, shift+tab, history. |
| `test/fixtures/fake-gh.mjs` | Stand-in `gh` for tests (set `GITCAT_GH_SHIM`): `repo create --source --push` and `repo view` backed by a local bare repo, so GitHub flows run end to end without a real account. |
| `scripts/eval.js` | 50-request accuracy/latency eval against real models. |
| `scripts/e2e.js` | 18 real-model scenarios on throwaway repos with git-state assertions. |
| `check_docs.py` | Regenerates the tree above and flags undocumented / phantom paths. |
| `check_docs.bat` | Double-click wrapper for `check_docs.py`. |
| `.env.example` | Every env var GitCat reads, with placeholders. |

## Docs and config

| Path | What it does |
|---|---|
| `README.md` | Three-line pitch + links. |
| `start_commands.md` | How to install, run, verify, reset — every command was run. |
| `docs/architecture.md` | Components, flows (Mermaid), data, boundaries. Source of truth for the HTML. |
| `docs/architecture.html` | Browsable render of architecture.md. Regenerate when the markdown changes. |
| `docs/decisions.md` | Append-only log of non-obvious choices (with measured numbers). |
| `docs/stack_and_tooling.md` | Stack, why each piece, rejected alternatives. |
| `docs/structure.md` | This file. |
| `package.json` | `npm test`, `npm run eval`, and the `gitcat` bin that `npm link` installs. |
